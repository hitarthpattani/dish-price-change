# TODO Inventory

Generated by `commerce-app-builder-generator` on 2026-07-31.

Source plan: `requirements/app-builder/Dish_PriceChange.md`

84 TODO markers across 27 generated source files. Implement in the order below — Foundation stubs
first (they unblock every flow), then per-flow handlers. 🔴 = blocked by an open Reinterpretation
item; resolve that item before implementing.

**Open Reinterpretation items** (see the plan's §11 and the reminder below):

- 🔴 **item 2** — Custom Pricing & Bundle Data Sources (BLOCKING): pricing/eligibility + bundle cache.
- 🔴 **item 3** — PBP / UMS JWT token acquisition.

## Progress

- [ ] Foundation TODOs (repositories, clients, shared libs): 43 items
- [ ] Flow 1 — Webhook Renewal Ingestion: 4 items
- [ ] Flow 2 — CSV Renewal Ingestion: 3 items
- [ ] Flow 3 — Price Change Execution: 26 items
- [ ] Flow 4 — Reporting Delivery: 2 items
- [ ] Flow 5 — Maintenance Sweeps: 4 items

---

## Foundation (do first — unblocks all flows)

**Database repositories**

- [ ] `lib/database/repository/sling-prerenewal-notifications/index.ts` (14) — implement the 7 queue
      methods (insertNotification, insertMany, findDueForPriceChange, markConsumed, incrementRetry,
      markNotToRetry, deleteFinalizedOlderThan) per plan §5.3.

**External integration clients**

- [ ] 🔴 `lib/adobe-commerce/catalog/index.ts` (6) — enabled package products, plan/offer pricing,
      bundle child associations (§5.4). **Blocked by item 2** (data source is custom Dish modules).
- [ ] 🔴 `lib/integrations/recurly/index.ts` (5) — fetch/change subscription (§5.4).
- [ ] 🔴 `lib/integrations/partner-billing/index.ts` (5) — getToken + batchPriceChange (§5.4).
      **item 3** (token source).
- [ ] 🔴 `lib/integrations/ums-reporting/index.ts` (3) — sendReport + 429 retry (§5.4). **item 3**.

**Shared library modules**

- [ ] `lib/utils/logger/index.ts` (4) — createLogger + logStep step-code catalog (§5.5).
- [ ] `lib/utils/params/index.ts` (4) — businessConfig access + request context (§5.5).
- [ ] `lib/utils/events-publisher/index.ts` (2) — publishInternalEvent (§5.5).
- [ ] `lib/utils/report-builder/index.ts` (2) — buildReportRow (§5.5).
- [ ] `lib/utils/errors/index.ts` — already implemented (no TODOs).

## Flow 1 — Webhook Renewal Ingestion

- [ ] `actions/renewal/notification/index.ts` (2) — feature flag, event_type filter, publish
      prerenewal.received (§6.4.a). Auth is IMS S2S (`require-adobe-auth: true`).
- [ ] `actions/external-events/pre-renewal-persist-consumer/index.ts` (2) — insert row; on failure
      report + publish reporting.queued (§6.4.a).

## Flow 2 — CSV Renewal Ingestion

- [ ] `actions/renewal/csv-import/index.ts` (2) — parse CSV, insertMany; on failure log + errors[]
      (item 6 = log-only, no AJO event) (§7.4.a).
- [ ] `web-src/src/components/RenewalImport/index.tsx` (1) — POST the CSV to `renewal/csv-import`
      with actionCallHeaders; render the result summary (§7.4.e).

## Flow 3 — Price Change Execution

**Actions**

- [ ] `actions/application-crons/price-change-scheduler/index.ts` (2) — gates, findDue, chunk,
      Openwhisk fan-out to price-change-worker (§8.4.a).
- [ ] 🔴 `actions/application-crons/price-change-worker/index.ts` (2) — the engine loop (§8.4.a).
      **items 2 & 3**.
- [ ] `actions/price-change/package-mapping/index.ts` (2) — GET/POST mapping CRUD (§8.4.e, item 8).

**Mapping storage (item 8 — resolved to ABDB)**

- [ ] `lib/database/repository/sling-renewal-package-mapping/index.ts` (6) — listByType, saveMapping,
      deleteMapping (§8.4.e).

**Flow lib modules**

- [ ] 🔴 `lib/price-change/eligibility/index.ts` (2) — checkEligibility (§8.4.d). **item 2**.
- [ ] `lib/price-change/change-request-builder/index.ts` (2) — buildChangeRequest (§8.4.d).
- [ ] 🔴 `lib/price-change/partner-billing-processor/index.ts` (2) — processBatchResponse (§8.4.d).
      **items 2 & 3**.
- [ ] `lib/price-change/retry/index.ts` (2) — classifyAndRecord (§8.4.d).
- [ ] 🔴 `lib/price-change/package-pricing/index.ts` (2) — getPlanAndOfferPricing (§8.4.d). **item 2**.

**Admin UI (mapping grids)**

- [ ] `web-src/src/components/PriceChangeConfig/components/ActiveMapping/index.tsx` (2) — grid load +
      form submit via package-mapping (§8.4.e). Date field is TEXT (no DATE FieldType).
- [ ] `web-src/src/components/PriceChangeConfig/components/PauseMapping/index.tsx` (2) — same, pause.

## Flow 4 — Reporting Delivery

- [ ] 🔴 `actions/external-events/reporting-delivery-consumer/index.ts` (2) — JWT, chunk, POST to UMS
      with 429 retry; no-op on blank endpoint (§9.4.a). **item 3** (UMS JWT).

## Flow 5 — Maintenance Sweeps

- [ ] `actions/application-crons/remove-renewal-notifications/index.ts` (2) — purge status=1 rows
      older than day_interval (§10.4.a).
- [ ] 🔴 `actions/application-crons/rebuild-bundle-guid-cache/index.ts` (2) — rebuild bundle GUID
      cache (§10.4.a). **Blocked by item 2**.

---

## Also remember

- Replace the `it`-stub tests with real behavior tests as you implement each file (they currently
  assert the placeholder success / TODO throw only, to hold the 100% coverage gate).
- Foundation `EXTENSION_ID` = `DishPriceChange` (in `actions/constants.ts` +
  `web-src/src/types/constants.ts`) — keep them identical.
