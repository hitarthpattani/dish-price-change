operations:
  view:
    - type: web
      impl: index.html
actions: actions
web: web-src
hooks:
  pre-app-build: hooks/pre-app-build.js
  pre-app-deploy: hooks/pre-app-deploy.js
runtimeManifest:
  packages:
    admin-ui-sdk:
      license: Apache-2.0
      actions:
        $include: actions/admin-ui-sdk/actions.config.yaml
    # Per-flow packages are merged in by Phase 3 builds:
    #   renewal-ingestion, commerce-events   — Flow 1 §6.4.b
    #   application-crons, price-change      — Flow 3 §8.4.b
