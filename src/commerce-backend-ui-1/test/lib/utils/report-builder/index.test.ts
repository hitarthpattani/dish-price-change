/*
 * <license header>
 */

import { buildReportRow } from '@lib/utils/report-builder'

describe('buildReportRow', () => {
  it('should throw TODO error', () => {
    expect(() => buildReportRow({}, true, undefined, 'success', 'step')).toThrow(
      'TODO: implement buildReportRow'
    )
  })
})
