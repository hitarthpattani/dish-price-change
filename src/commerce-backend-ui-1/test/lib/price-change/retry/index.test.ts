/*
 * <license header>
 */

import { classifyAndRecord } from '@lib/price-change/retry'

describe('classifyAndRecord', () => {
  it('should throw TODO error', async () => {
    await expect(classifyAndRecord([])).rejects.toThrow('TODO: implement classifyAndRecord')
  })
})
