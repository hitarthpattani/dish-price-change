/*
 * <license header>
 */

import { getPlanAndOfferPricing } from '@lib/price-change/package-pricing'

describe('getPlanAndOfferPricing', () => {
  it('should throw TODO error', async () => {
    await expect(getPlanAndOfferPricing('renewal.scheduled', 'PLAN-1')).rejects.toThrow(
      'TODO: implement getPlanAndOfferPricing'
    )
  })
})
