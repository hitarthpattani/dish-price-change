/*
 * <license header>
 */

import { buildChangeRequest } from '@lib/price-change/change-request-builder'

describe('buildChangeRequest', () => {
  it('should throw TODO error', () => {
    expect(() => buildChangeRequest({}, {}, false)).toThrow('TODO: implement buildChangeRequest')
  })
})
