/*
 * <license header>
 */

import { processBatchResponse } from '@lib/price-change/partner-billing-processor'

describe('processBatchResponse', () => {
  it('should throw TODO error', () => {
    expect(() => processBatchResponse({}, [])).toThrow('TODO: implement processBatchResponse')
  })
})
