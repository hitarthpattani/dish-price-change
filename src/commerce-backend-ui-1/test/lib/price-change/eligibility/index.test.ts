/*
 * <license header>
 */

import { checkEligibility } from '@lib/price-change/eligibility'

describe('checkEligibility', () => {
  it('should throw TODO error', () => {
    expect(() => checkEligibility({}, {}, {})).toThrow('TODO: implement checkEligibility')
  })
})
